import 'package:flutter/material.dart';

class PlanningScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Planification de messages/cours')),
      body: Center(
        child: Text('Écran de planification'),
      ),
    );
  }
}